scalar Date
scalar MongoObjectId

enum RecordStatus {
  Activate
  Pending
  Deactivate
}

enum RequestStatus {
  Success
  Failed
}

enum SortOrder {
  ascending
  descending
}

type MutationSuccessResponse {
  message: String!
  status: RequestStatus!
}
